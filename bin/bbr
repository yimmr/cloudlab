#!/bin/bash

# =================================================================
# 海外非 CN2 线路 Hysteria 2 (UDP/QUIC) 极限性能优化脚本
# Updated: 2026-01
# =================================================================

# 权限检查
[[ $EUID -ne 0 ]] && echo "❌ 错误: 请使用 root 权限运行！" && exit 1

echo "🚀正在写入 Hysteria 2 专属优化配置..."

cat > /etc/sysctl.d/99-hy2-optimize.conf << EOF
# 核心：UDP/TCP 缓冲区 (防止小内存 OOM)
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
net.core.rmem_default = 16777216
net.core.wmem_default = 16777216

# TCP 缓冲区限制
net.ipv4.tcp_rmem = 4096 87380 16777216
net.ipv4.tcp_wmem = 4096 65536 16777216

# 拥塞控制与队列
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr

# UDP/QUIC 专项优化
net.core.netdev_max_backlog = 50000
net.core.somaxconn = 65535
net.ipv4.tcp_mtu_probing = 1
# net.ipv4.tcp_base_mss = 1024

# 降低延迟与连接优化
net.ipv4.tcp_notsent_lowat = 16384
net.ipv4.tcp_slow_start_after_idle = 0
net.ipv4.tcp_tw_reuse = 1
# net.ipv4.tcp_fastopen = 3
EOF

echo "🚀 正在设置系统文件句柄限制 (ULIMIT)..."
# 不去改 /etc/security/limits.conf 主文件，而是写入独立文件
cat > /etc/security/limits.d/99-hy2-limits.conf << EOF
* soft nofile 1048576
* hard nofile 1048576
root soft nofile 1048576
root hard nofile 1048576
* soft nproc 512000
* hard nproc 512000
root soft nproc 512000
root hard nproc 512000
EOF
echo "✅ 已创建 /etc/security/limits.d/99-hy2-limits.conf"

# 确保 systemd 服务也生效
sed -i 's/^#DefaultLimitNOFILE=.*/DefaultLimitNOFILE=1048576/' /etc/systemd/system.conf
sed -i 's/^DefaultLimitNOFILE=.*/DefaultLimitNOFILE=1048576/' /etc/systemd/system.conf

sysctl --system

echo "=================================================="
echo "✅ 优化完成！建议等其他任务完成后重启服务器"
echo "=================================================="